services:
  kostinbrod-hunt:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:3040:3000"
    restart: unless-stopped
    volumes:
      - ./data:/usr/src/app/data